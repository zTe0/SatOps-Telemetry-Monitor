services:
  - type: web
    name: satops-telemetry-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt && python generate_sample_data.py
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
